# 🚀 시간표 앱 배포 가이드

이 문서는 시간표 앱을 GitHub와 Firebase에 업로드하는 방법을 단계별로 설명합니다.

---

## 📋 목차
1. [GitHub에 업로드하기](#1-github에-업로드하기)
2. [Firebase에 배포하기](#2-firebase에-배포하기)
3. [자동 배포 설정 (GitHub Actions)](#3-자동-배포-설정-github-actions)

---

## 1. GitHub에 업로드하기

### 1.1 GitHub 계정 정보
- **GitHub 사용자명**: `hyunsdaddy-moon`
- **저장소 주소**: https://github.com/hyunsdaddy-moon/my-timetable.git

### 1.2 Git 설정하기 (최초 1회만)

터미널을 열고 다음 명령어를 실행합니다:

```bash
# Git 사용자 이름 설정
git config --global user.name "hyunsdaddy-moon"

# Git 이메일 설정 (GitHub 계정 이메일)
git config --global user.email "your-email@example.com"
```

> ⚠️ **주의**: `your-email@example.com`을 실제 GitHub 계정 이메일로 바꾸세요!

### 1.3 GitHub에 코드 업로드하기

#### 방법 1: 터미널 사용 (현재 설정된 방법)

```bash
# 1. 프로젝트 폴더로 이동
cd "/Users/hyunsdaddy/Library/Mobile Documents/com~apple~CloudDocs/Antygravity/시간표 앱"

# 2. 변경된 파일 확인
git status

# 3. 변경된 파일을 스테이징 영역에 추가
git add .

# 4. 커밋 메시지와 함께 저장
git commit -m "변경 내용 설명을 여기에 작성"

# 5. GitHub에 업로드
git push
```

#### 방법 2: VS Code 사용

1. VS Code에서 프로젝트 폴더 열기
2. 왼쪽 사이드바에서 **Source Control** 아이콘 클릭 (또는 `Ctrl+Shift+G`)
3. 변경된 파일 옆의 **+** 버튼 클릭하여 스테이징
4. 상단 입력창에 커밋 메시지 입력
5. **✓ Commit** 버튼 클릭
6. **⋯** 메뉴 → **Push** 선택

### 1.4 GitHub 인증 방법

GitHub에 처음 push할 때 인증이 필요합니다:

#### Personal Access Token (권장)

1. GitHub 웹사이트 접속 → 로그인
2. 우측 상단 프로필 → **Settings**
3. 왼쪽 메뉴 맨 아래 **Developer settings**
4. **Personal access tokens** → **Tokens (classic)**
5. **Generate new token** → **Generate new token (classic)**
6. 설정:
   - Note: `시간표 앱 배포`
   - Expiration: `No expiration` (또는 원하는 기간)
   - 권한: `repo` 전체 체크
7. **Generate token** 클릭
8. 생성된 토큰을 복사 (⚠️ 다시 볼 수 없으니 안전한 곳에 저장!)

9. 터미널에서 push할 때:
   - Username: `hyunsdaddy-moon`
   - Password: **복사한 토큰 붙여넣기**

---

## 2. Firebase에 배포하기

### 2.1 Firebase CLI 설치

터미널에서 다음 명령어를 실행합니다:

```bash
# Node.js가 설치되어 있어야 합니다
npm install -g firebase-tools
```

### 2.2 Firebase 로그인

```bash
firebase login
```

브라우저가 열리면 Google 계정으로 로그인합니다.

### 2.3 Firebase 프로젝트 초기화

```bash
# 1. 프로젝트 폴더로 이동
cd "/Users/hyunsdaddy/Library/Mobile Documents/com~apple~CloudDocs/Antygravity/시간표 앱"

# 2. Firebase 초기화
firebase init
```

초기화 과정에서 다음과 같이 선택합니다:

1. **어떤 Firebase 기능을 사용하시겠습니까?**
   - `Hosting` 선택 (스페이스바로 선택, 엔터로 확인)

2. **프로젝트 설정**
   - `Use an existing project` 선택
   - 기존 Firebase 프로젝트 선택 (또는 새로 생성)

3. **Public directory는 무엇입니까?**
   - `.` (현재 폴더) 입력 후 엔터

4. **단일 페이지 앱으로 구성하시겠습니까?**
   - `No` 선택

5. **자동 빌드 및 배포 설정하시겠습니까?**
   - `Yes` 선택 (GitHub Actions 자동 배포)
   - GitHub 저장소: `hyunsdaddy-moon/my-timetable`

### 2.4 Firebase 배포하기

```bash
# Firebase에 배포
firebase deploy
```

배포가 완료되면 **Hosting URL**이 표시됩니다:
```
✔  Deploy complete!

Hosting URL: https://your-project-id.web.app
```

### 2.5 Firebase 프로젝트 정보 확인

```bash
# 현재 프로젝트 확인
firebase projects:list

# 프로젝트 상태 확인
firebase deploy --only hosting --dry-run
```

---

## 3. 자동 배포 설정 (GitHub Actions)

Firebase 초기화 시 자동 배포를 선택했다면, GitHub에 코드를 push할 때마다 자동으로 Firebase에 배포됩니다.

### 3.1 자동 배포 확인

프로젝트 폴더에 다음 파일이 생성되었는지 확인:
- `.github/workflows/firebase-hosting-merge.yml` (main 브랜치에 push 시)
- `.github/workflows/firebase-hosting-pull-request.yml` (PR 생성 시)

### 3.2 자동 배포 작동 방식

1. 코드 수정 후 GitHub에 push
   ```bash
   git add .
   git commit -m "업데이트 내용"
   git push
   ```

2. GitHub Actions가 자동으로 실행됨

3. 1-2분 후 Firebase에 자동 배포 완료

4. 배포 상태 확인:
   - GitHub 저장소 → **Actions** 탭
   - Firebase Console → **Hosting** 섹션

---

## 📝 빠른 참조 명령어

### GitHub 업로드
```bash
cd "/Users/hyunsdaddy/Library/Mobile Documents/com~apple~CloudDocs/Antygravity/시간표 앱"
git status                          # 변경 사항 확인
git add .                           # 모든 변경 사항 추가
git commit -m "변경 내용"           # 커밋
git push                            # GitHub에 업로드
```

### Firebase 배포
```bash
cd "/Users/hyunsdaddy/Library/Mobile Documents/com~apple~CloudDocs/Antygravity/시간표 앱"
firebase deploy                     # Firebase에 배포
```

### 상태 확인
```bash
git status                          # Git 상태
git log --oneline -5                # 최근 커밋 5개 확인
firebase projects:list              # Firebase 프로젝트 목록
```

---

## 🔧 문제 해결

### 1. Git push가 실패하는 경우

**에러**: `Authentication failed`

**해결**:
1. Personal Access Token 재생성
2. 터미널에서 저장된 자격 증명 삭제:
   ```bash
   git credential-osxkeychain erase
   host=github.com
   protocol=https
   [Enter 두 번]
   ```
3. 다시 push 시도

### 2. Firebase 배포가 실패하는 경우

**해결**:
```bash
# 다시 로그인
firebase logout
firebase login

# 프로젝트 재선택
firebase use --add

# 다시 배포
firebase deploy
```

### 3. 파일이 너무 많아서 느린 경우

`.gitignore` 파일 생성 및 불필요한 파일 제외:
```
node_modules/
.DS_Store
*.log
.firebase/
```

---

## 🎯 현재 설정 요약

- **GitHub 저장소**: https://github.com/hyunsdaddy-moon/my-timetable.git
- **사용자명**: hyunsdaddy-moon
- **브랜치**: main
- **Firebase Hosting**: (초기화 필요)

---

## 📞 추가 도움말

- **GitHub 문서**: https://docs.github.com/ko
- **Firebase 문서**: https://firebase.google.com/docs/hosting
- **Git 기초**: https://git-scm.com/book/ko/v2

---

**마지막 업데이트**: 2026-03-06
