# 🚀 나의 시간표 앱 우주로 쏘아올리기 대작전! (상세 버전)

우리가 만든 시간표 앱이 전 세계 사람들이 핸드폰으로 볼 수 있게 되기까지 어떤 마법 같은 일들이 있었는지 차근차근 정리해 보았어요! ✨

이 과정은 마치 **1) 내 방(컴퓨터)에 있는 예쁜 그림을 2) 안전한 보물창고(GitHub)에 맡겨두고 3) 모두가 언제든 볼 수 있는 갤러리(Firebase)에 전시하는 것과 같아요.**

---

## 🧸 1단계: 내 보물창고, GitHub에 코드 저장하기

가장 먼저 우리가 작성한 HTML, CSS, JavaScript 코드들을 원격 저장소인 GitHub(깃허브)에 올려야 했어요. GitHub는 코드를 안전하게 보관해 주는 보물창고 같은 곳이랍니다.

### 🏠 GitHub 홈페이지에서 창고 만들기 상세 레시피
1. **GitHub 접속 및 로그인:** 인터넷 창을 열고 구글에 'GitHub'를 검색해서 들어간 다음, 선생님의 아이디(`hyunsdaddy-moon`)로 로그인해요.
2. **새로운 보물창고 만들기 버튼 클릭:** 화면 제일 위 오른쪽 구석에 있는 **[+] (더하기 모양) 버튼**을 누릅니다. 그러면 작은 메뉴가 뚝 떨어지는데, 거기서 **[New repository (새 저장소)]** 를 클릭하세요!
3. **창고 이름 짓기:** 화면이 바뀌면 `Repository name (저장소 이름)` 칸에 우리가 만들 앱 이름인 **`my-timetable`** 이라고 적어줍니다.
4. **공개 여부 선택하기:** 다른 사람들도 내 코드를 볼 수 있게 **[Public (퍼블릭, 공개)]** 옵션에 🔵 파란 불이 들어오게 동그라미를 체크합니다. (만약 비밀로 하고 싶다면 Private을 눌러도 돼요!)
5. **창고 생성!:** 맨 아래에 있는 초록색 **[Create repository (저장소 만들기)]** 버튼을 꾹 누르면, 짠! 선생님만의 텅 빈 보물창고가 생겼어요!

### 💻 내 컴퓨터 창고와 GitHub 창고 연결하기
1. 컴퓨터에 있는 우리 앱 폴더(`시간표 앱`)에서 까만 화면(터미널)을 엽니다.
2. "이제부터 이 폴더의 변경 사항을 추적할 거야!"라고 컴퓨터에게 먼저 명령합니다. (`git init`)
3. 그리고 방금 화면에 뜬 빈 창고 주소를 복사해서 붙여넣고 연결해 줍니다. (`git remote add origin https://github.com/hyunsdaddy-moon/my-timetable.git`)
4. 지금까지 짠 코드들을 상자에 담아(`git add .`) 예쁜 이름표를 붙이고 (`git commit -m "First commit"`), 마지막으로 내 창고 안으로 힘껏 밀어 넣어요! (`git push -u origin main`)

---

## 🗄️ 2단계: Firebase에서 실시간 데이터베이스 만들기

우리의 시간표가 여러 기기에서 동시에 똑같이 바뀌려면 인터넷 어딘가에 거대한 파일 저장소가 필요해요. 우리는 구글의 **Firebase(파이어베이스)** 라는 곳을 선택했어요!

### 🔥 Firebase 홈페이지에서 프로젝트(집) 짓기 상세 레시피
1. **Firebase 접속 및 로그인:** 구글 사이트에 'Firebase'를 치고 들어가서 오른쪽 위에 있는 **[시작하기 (Go to console)]** 버튼을 클릭합니다. 선생님의 구글 계정(`neuweg73@gmail.com`)으로 로그인 되어 있어야 해요!
2. **집터 찾기:** 커다란 하얀 상자인 **[+ 프로젝트 추가 (Add project)]** 버튼을 누릅니다.
3. **집 이름 짓기:** 프로젝트 이름 칸에 **`my-school-timetable`** 이라고 입력하고 **[계속]** 버튼을 누릅니다.
4. **애널리틱스 설정:** 구글 애널리틱스 사용 설정 화면이 나오는데, 이 기능은 누가 얼마나 들어오는지 분석하는 거라 지금은 꺼두어도 괜찮습니다. **버튼을 눌러 회색으로 끄고 [프로젝트 만들기]** 버튼을 누릅니다. 조금만 기다리면 집 짓기 완성!

### 📚 실시간 데이터베이스(Realtime Database) 연결하기 상세 레시피
1. **데이터베이스 만들기:** 프로젝트 화면 왼쪽 메뉴에서 **[빌드 (Build)]** 글자를 누르면 서랍이 열립니다. 거기서 **[Realtime Database]** 를 클릭하고, 가운데 있는 **[데이터베이스 만들기 (Create database)]** 버튼을 꾹 누릅니다.
2. **장소 선택:** 데이터가 저장될 장소를 고르게 나오는데 기본값(미국 또는 싱가포르)으로 두고 **[다음]** 을 누릅니다.
3. **✨아주 중요한 보안 규칙 설정:** 시작 모드 선택 화면이 나옵니다. 무조건 **[테스트 모드에서 시작 (Start in test mode)]** 에 체크 동그라미를 활성화해야 해요! 그래야 내 컴퓨터에서 마음껏 데이터를 저장하고 읽을 수 있습니다. 경고가 뜨지만 괜찮습니다. **[사용 설정 (Enable)]** 을 누르면 완성!
4. **마법의 열쇠 다운받기:**
   - 다시 왼쪽 위 톱니바퀴 ⚙️ 모양 설정 버튼을 누르고 **[프로젝트 설정 (Project settings)]** 에 들어갑니다.
   - 스크롤을 아주아주 맨 밑으로 내리면 "내 앱(Your apps)"이라는 칸이 있어요. 화면 가운데 있는 `< / >` 모양 아이콘(웹 앱)을 톡 하고 누릅니다.
   - 앱 닉네임을 **`timetable-web`** 으로 적고, 'Firebase 호스팅 설정' 체크박스는 일단 비워두고 **[앱 등록 (Register app)]** 버튼을 누릅니다.
   - 그러면 짜잔! 화면에 코드 모양의 **`firebaseConfig`** 라는 마법의 열쇠 뭉치가 나타나요.
   - 이 뭉치를 드래그해서 예쁘게 복사한 다음, 선생님 컴퓨터의 `script.js` 파일 맨 위에 붙여넣기 하면 연결이 완료됩니다!

---

## 🚀 3단계: 우리 동네 갤러리에 웹사이트 배포하기 (Firebase Hosting)

코드도 안전하게 보관했고 데이터도 연결했으니, 이제 누구나 인터넷 주소만 치면 들어올 수 있게 마법의 갤러리에 웹사이트를 조립해 띄워줄 차례예요!

1. **내 컴퓨터 터미널에서 Firebase 도구 설치하기:**
   * 터미널 창(까만 화면)에서 Firebase 마법사들을 컴퓨터 구석구석 설치해 줘야 합니다.
   * "Firebase CLI"라는 명령어를 사용해 내 컴퓨터가 Firebase와 대화할 수 있게 만들었어요.

2. **터미널을 통해 구글 계정으로 로그인 (인증):**
   * 터미널 창에 `npx firebase-tools login --no-localhost`라고 적고 엔터를 친 후 시작했죠!
   * 화면에 인터넷 주소(URL)가 나타나면 그걸 마우스로 복사해서 인터넷 주소창에 붙여넣고 들어갔어요.
   * 선생님 구글 계정으로 로그인하자마자 **[허용 (Allow)]** 버튼을 눌러 승인하니 화면 맨 마지막에 엄청나게 긴~ 기호와 숫자가 합쳐진 **비밀 인증 코드 조각(Authorization code)** 이 나왔답니다.
   * 그 긴 코드를 복사해서 채팅창에 쏙 넘겨주셔서, 제가 그것을 터미널 인증창에 척~ 하고 넣었더니 로그인 문이 딸깍~ 하고 열렸어요!

3. **우주선 발사 준비 및 배포 (Hosting):**
   * 로그인 문이 열리자마자, 제(Antigravity)가 Firebase가 길을 잃지 않도록 `firebase.json`과 `.firebaserc` 라는 배포용 환경설정 지도를 터미널 폴더 안에 척척 그려주었어요.
   * 이 지도를 통해 앱의 방향을 `my-school-timetable-f8cdd`로 잡았죠.
   * 마지막으로 로켓 발사 버튼인 `npx firebase-tools deploy` 마법의 주문을 외우자, 폴더 안에 있던 선생님의 121개 앱 구성 파일이 우주를 날아 Firebase 호스팅 갤러리로 슈웅~~ 전송되었어요.

4. **✨ 전시 완료! ✨**
   * 이제 **https://my-school-timetable-f8cdd.web.app** 이라는 아주 멋진 고유 주소가 생성되어, 이 주소만 복사하면 언제 어디서나 인터넷 창에서 친구들과 새로운 시간표를 확인할 수 있게 된 거예요! 🎉

---

**한 줄 요약!**
**GitHub**에는 내 파일을 꼼꼼히 저장(백업)했고, **Firebase 갤러리 웹주소**에는 모두가 열어볼 수 있도록 전시를 마쳤으며, 그 뒤엔 **Firebase 데이터베이스**가 우리가 칠판을 바꿀 때마다 척척 기억해 주는 실시간 노트가 되어주는 완벽한 삼총사가 완성되었습니다! 👏
