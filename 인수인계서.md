# 🏫 1학기 나의 시간표 ✨ - 프로젝트 인수인계서

## 1. 프로젝트 개요
* **프로젝트명**: 다이어리 스타일의 반응형 웹 시간표 앱
* **대상 사용자**: 중·고등학생
* **주요 목적**: 학생들이 자신만의 다이어리를 꾸미듯 직관적이고 감성적인 디자인으로 주간 시간표와 교시별 수업 시간을 기록할 수 있도록 제공합니다.
* **주요 기술 스택**: HTML5, CSS3, Vanilla JavaScript (외부 프레임워크 없는 순수 웹 기술)

---

## 2. 파일 구조 및 역할

| 파일명 | 역할 설명 |
|---|---|
| `index.html` | 앱의 뼈대(구조). 상단 제목 창, 시간표가 그려지는 영역 껍데기, 하단 네비게이션, 2개의 팝업창(과목 입력창/교시 시간 입력창) 태그 보관. **`<script type="module">`** 방식으로 스크립트를 불러옵니다. |
| `style.css` | 앱의 다이어리 컨셉 디자인 담당. 나눔펜글씨, 고운돋움 폰트, 파스텔톤 컬러 팔레트 배색, 클릭 및 팝업창 애니메이션 등 모든 예쁜 시각적 효과 포함. |
| `script.js` | 앱의 두뇌(핵심 로직). 빈 시간표를 그리고, 팝업창을 제어하며, 입력받은 데이터를 저장하고, 화면을 즉시 갱신(Render)하는 모든 자바스크립트 모듈 코드가 담겨 있습니다. |

---

## 3. 핵심 기능 명세

1. **빈 시간표 자동 초기화 로직**
   - 최초 실행 시 월~금, 1~8교시에 해당하는 `scheduleData` 배열을 2차원으로 생성하며 모두 '자습'으로 채웁니다. 
   - 4교시 종료 후엔 자동 '맛있는 점심' 바(Bar)를 출력합니다.

2. **다이어리 클릭 이벤트 팝업(Edit Modal)**
   - 요일별 어떤 시간표 칸이든 클릭하면 과목명, 선생님 이름, 예쁜 배경색 6가지 중 하나를 고를 수 있는 입력창이 나타납니다.

3. **교시 시간 기록 기능 (Time Modal)**
   - 화면 왼쪽 핑크색 동그라미 교시 숫자(1, 2, 3...)를 클릭하면, 그 교시의 정확한 수업 시간(예: 09:00 - 09:50)을 입력할 수 있는 전용 팝업창이 나타납니다.

---

## 4. 데이터 저장 기술 작동 원리 (매우 중요 🌟)

현재 이 앱은 선생님께서 직접 테스트하실 수 있도록 기발한 방식의 저장/동기화 기술이 병합되어 있습니다.

### [Phase 1] 내 방 서랍의 방식 (LocalStorage)
* 기존 동작 방식으로, 인터넷 없이도 내 크롬 창 안에 데이터를 평생 안전하게 보관합니다. `localStorage.setItem`을 통해 과목정보와 교시시간을 각각 분리 저장합니다.

### [Phase 2] 실시간 마법 동기화 기술 적용 (Storage Event)
* 현재 코드에는 완벽한 동기화 테스트를 위한 코드가 심어져 있습니다. 크롬 브라우저 창을 2개 나란히 켜놓고, 한쪽 창에서 과목이나 시간을 변경/저장해 보세요.
* `window.addEventListener('storage', ...)` 리스너가 작동하여, **옆에 켜져 있던 변경 전 시간표(다른 창)가 새로고침 조작 없이도 실시간으로 즉시 입력된 내용으로 변신**하는 마법 같은 경험을 제공합니다!
* 이것이 클라우드 데이터베이스(Firebase)가 여러 폰에서 완벽하게 작동하는 근본 원리입니다.

---

## 5. 🚀 향후 작업 가이드 (스마트폰 연동)

이 소스폴더를 받으시는 분(또는 이후의 개발)은 당장이라도 선생님의 폰과 컴퓨터의 데이터를 완벽 연동할 수 있습니다.

1. 인터넷에 `Firebase(구글)`를 검색해 무료 계정을 만듭니다.
2. 새 프로젝트를 생성하고 실시간 데이터베이스(Realtime Database)를 활성화합니다.
3. `script.js` 맨 위에 있는 아래 코드를 선생님(개발자) 개인의 주소로만 딱 바꾸어 넣으세요!

```javascript
const firebaseConfig = {
  // 선생님이 Firebase 가입 후 받는 고유 주소로 이 한 줄만 교체하세요!
  databaseURL: "https://my-real-project-name.firebaseio.com" 
};
```

이렇게 한 줄만 바꾸어 배포하시면, 이 세상 어디에 있는 기기든 똑같은 시간표를 실시간으로 가져와 보여주는 **상용 서비스 앱 수준**의 프로젝트로 업그레이드됩니다!
