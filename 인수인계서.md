# 🏫 1학기 나의 시간표 ✨ - 프로젝트 인수인계서

## 1. 프로젝트 개요
* **프로젝트명**: 다이어리 스타일의 반응형 웹 시간표 앱
* **대상 사용자**: 중·고등학생
* **주요 목적**: 학생들이 자신만의 다이어리를 꾸미듯 직관적이고 감성적인 디자인으로 주간 시간표와 교시별 수업 시간을 기록할 수 있도록 제공합니다.
* **주요 기술 스택**: HTML5, CSS3, Vanilla JavaScript (외부 프레임워크 없는 순수 웹 기술)

---

## 2. 파일 구조 및 역할

| 파일명 | 역할 설명 |
|---|---|
| `index.html` | 앱의 뼈대(구조). 상단 제목 창, 시간표가 그려지는 영역 껍데기, 하단 네비게이션, 2개의 팝업창(과목 입력창/교시 시간 입력창) 태그 보관. **`<script type="module">`** 방식으로 스크립트를 불러옵니다. |
| `style.css` | 앱의 다이어리 컨셉 디자인 담당. 나눔펜글씨, 고운돋움 폰트, 파스텔톤 컬러 팔레트 배색, 클릭 및 팝업창 애니메이션 등 모든 예쁜 시각적 효과 포함. |
| `script.js` | 앱의 두뇌(핵심 로직). 빈 시간표를 그리고, 팝업창을 제어하며, 입력받은 데이터를 저장하고, 화면을 즉시 갱신(Render)하는 모든 자바스크립트 모듈 코드가 담겨 있습니다. |

---

## 3. 핵심 기능 명세

1. **빈 시간표 자동 초기화 로직**
   - 최초 실행 시 월~금, 1~8교시에 해당하는 `scheduleData` 배열을 2차원으로 생성하며 모두 '자습'으로 채웁니다. 
   - 4교시 종료 후엔 자동 '맛있는 점심' 바(Bar)를 출력합니다.

2. **다이어리 클릭 이벤트 팝업(Edit Modal)**
   - 요일별 어떤 시간표 칸이든 클릭하면 과목명, 선생님 이름, 예쁜 배경색 6가지 중 하나를 고를 수 있는 입력창이 나타납니다.

3. **교시 시간 기록 기능 (Time Modal)**
   - 화면 왼쪽 핑크색 동그라미 교시 숫자(1, 2, 3...)를 클릭하면, 그 교시의 정확한 수업 시간(예: 09:00 - 09:50)을 입력할 수 있는 전용 팝업창이 나타납니다.

---

## 4. 데이터 저장 기술 작동 원리 (완성)

현재 이 앱은 데스크톱(PC)과 모바일 기기(아이폰/아이패드 등)에서 완벽한 실시간 동기화가 이루어지도록 **Firebase Realtime Database**가 탑재되어 있습니다.

### [Phase 1] 로컬 저장소(초기 로딩 및 백업)
* 인터넷 연결 대기 중이거나 오프라인 상태일 때를 대비해 크롬 브라우저의 `localStorage`를 사용하여 기본 데이터를 보존합니다.

### [Phase 2] 실시간 클라우드 동기화 (Firebase 적용)
* `script.js` 내에 프로젝트와 연동된 고유 Firebase URL이 적용되어 있습니다.
* `timetableRef.on('value', ...)` 이벤트 리스너가 작동하여, **폰에서 시간표 데이터를 변경(추가/삭제)하면 컴퓨터 화면이 새로고침 없이도 즉시 실시간으로 변신**하는 마법 같은 경험을 제공합니다.
* 다수의 기기에서 접속하더라도 최신 데이터 상태로 자동 유지됩니다.

---

## 5. 🚀 앱(PWA) 배포 및 사용 가이드

이 소스코드는 현재 GitHub 계정과 연동되어 **GitHub Pages**를 통해 누구나 접속 가능한 상용 웹사이트로 배포되었습니다.

### 📱 아이폰(iOS)에서 '진짜 앱'처럼 사용하는 방법 (PWA 적용)
`index.html`과 새롭게 추가된 `manifest.json` 파일의 상호작용으로 모바일 기기 홈 화면에 웹앱을 설치할 수 있습니다.

1. 아이폰(또는 아이패드)의 **Safari(사파리)** 브라우저를 열고 배포된 앱 주소로 접속합니다.
   👉 (예: `https://[깃허브아이디].github.io/my-timetable/`)
2. 하단 메뉴 중앙의 **[공유(내보내기) 📤]** 아이콘을 탭합니다.
3. 스크롤을 내려 **`[홈 화면에 추가]`** 를 선택합니다.
4. 오른쪽 상단의 **[추가]** 를 누르면 아이폰 배경화면에 예쁜 타이틀과 아이콘이 추가됩니다!
5. 설치된 아이콘을 눌러 실행하면, Safari 주소창이 사라진 **전체화면 모드(Standalone)**로 실제 어플리케이션처럼 쾌적하게 사용할 수 있습니다.
